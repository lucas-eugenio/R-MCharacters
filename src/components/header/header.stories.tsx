import React from "react";
import Header from "./header.component";

export default {
  title: "Storybook/Header",
  component: Header,
};

export const Default = (): React.ReactElement => <Header />;
